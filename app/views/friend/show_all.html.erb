<% if @friends_list.empty? %>
  <h1 class="text-center">Sad Life, you dont have any friends 🤣</h1>
<% else %>
  <%# <h1 class="text-center">Damn Bruh, you have a total of <@friends_list.length> friends.</h1> %>

  <div class="row mt-4">
    <div class="col-4 alert alert-info text-center"><h1>Name</h1></div>
    <div class="col-4 alert alert-info text-center"><h1>Email</h1></div>
    <div class="col-4 alert alert-info text-center"><h1>Phone Number</h1></div>
  </div>

  <% @friends_list.each do |friend| %> 
    <div class="row p-4 border">
      <div class="col-4 text-center"><%=friend.name%></div>
      <div class="col-4 text-center"><%=friend.email%></div>
      <div class="col-4 text-center"><%=friend.phone_number%></div>
    </div>
  <% end %> 
<% end %>
